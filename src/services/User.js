// import Fetch API method
import Fetch from "../utils/Fetch"

const updateUserProfile = async ({ username, password }) => {   
   // Json object
   // let dataObj = {
   //    username,
   //    password,
   //    "grant_type": 'password',
   //    "client_id": '7U32R6-OU90V4-7Z4CUC-O25XZK5F-CZI',
   // }
   // console.log(params.toString());   
   
   // return result to endpoint
   // return await Fetch('endpoint', 'POST', params.toString(), true);
   
}

export const UserService = {
   updateUserProfile
}